Go to **Settings → Technical → Timesheets** and set the **Time Connecting** option:

- **Set Start Date to Now** (default): new timesheet entries start at the current time.
- **Align to previous entry**: new entries start immediately after the last recorded entry for the same employee on the same day.
