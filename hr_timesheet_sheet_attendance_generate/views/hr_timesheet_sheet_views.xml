<?xml version='1.0' encoding='utf-8' ?>
<odoo>
    <record id="hr_timesheet_sheet_form_attendance_generate" model="ir.ui.view">
        <field name="name">hr.timesheet.sheet.form.attendance_generate</field>
        <field name="model">hr_timesheet.sheet</field>
        <field name="inherit_id" ref="hr_timesheet_sheet.hr_timesheet_sheet_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//header/button[@name='action_timesheet_confirm']"
                position="after"
            >
                <button
                    name="button_generate_attendances"
                    string="Generate Attendances"
                    type="object"
                    invisible="state != 'draft'"
                    help="Generate attendances for dates which have imputed hours in the timesheet sheet but no attendances. The attendances are generated using the employee's working hours calendar."
                    groups="hr_attendance.group_hr_attendance_own_reader"
                />
            </xpath>
        </field>
    </record>
</odoo>
