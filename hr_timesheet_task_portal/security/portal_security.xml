<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">

        <record id="portal_task_analytic_timesheet_rule" model="ir.rule">
            <field name="name">Project/Task/Analytic Timesheet: portal users: public or (portal and colleagues following) or (followers and following)</field>
            <field name="model_id" ref="project.model_hr_analytic_timesheet"/>
            <field name="domain_force">[
            '|',
                '|',
                    '|',
                        ('task_id.project_id.privacy_visibility', '=', 'public'),
                        '&amp;',
                            ('task_id.project_id.privacy_visibility', '=', 'portal'),
                            ('task_id.project_id.message_follower_ids', 'child_of', [user.partner_id.commercial_partner_id.id]),
                    '&amp;',
                        ('task_id.project_id.privacy_visibility', '=', 'followers'),
                        ('task_id.project_id.message_follower_ids', 'in', [user.partner_id.id]),
                '&amp;',
                    # on employee project can receive messages but not access the object
                    ('task_id.project_id.privacy_visibility', '!=', 'employees'),
                    ('task_id.message_follower_ids', 'in', [user.partner_id.id])
            ]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        </record>

    </data>
</openerp>
